// Configuration for particle system
export const PARTICLE_CONFIG = {
  COUNT: 200, // Reduced for better performance
  SIZE: 0.04, // Increased for better visibility
  OPACITY: 0.8,
  CONNECTION_RADIUS: 1.2,
  ANIMATION_SPEED: 0.05, // Reduced for smoother animation
  MOVEMENT_RANGE: 0.001 // Reduced for subtler movement
} as const;